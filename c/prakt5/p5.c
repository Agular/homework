#include <stdio.h>
#include <stdlib.h>
#include <math.h>

double userSin (double x){
    double sum = x;
    
    for(int)
    return x;
}

int main(){
    double x;
    printf("Enter the variable for x for sin(x): ");
    scanf("%lf",&x);
    printf("The system function value of sin(%lf): %.4lf\n",x,sin(x));
    printf("The user function value of sin(%lf): %.4lf\n",x,userSin(x));
    return 0;
}
